<div class="card mb-1 post-body">
  <div class="card-body">
    <h5 class="card-title">
      <%= post.username %>
      <small>- <%= time_ago_in_words post.created_at %> ago</small>
    </h5>
    <div class="card-text lead mb-2"><%= post.body %></div>
    <%= link_to "#", class: "card-link", data: { reflex: "click->PostsReflex#repost", id: post.id } do %>
      <i class="fas fa-redo-alt"></i> (<%= tag.span post.reposts_count, id: "post-#{post.id}-reposts" %>)
    <% end %>
    <%= link_to "#", class: "card-link", data: { reflex: "click->PostsReflex#like", id: post.id } do  %>
      <i class="fas fa-thumbs-up"></i> (<%= tag.span post.likes_count, id: "post-#{post.id}-likes" %>)
    <% end %>
  </div>
</div>